[build]
  command = "npm run build"
  publish = ".next"

# Netlify Functions: scheduled scraper/generator + on-demand helpers
[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
  external_node_modules = ["cheerio"]

[[plugins]]
  package = "@netlify/plugin-nextjs"